import * as React from 'react'

type Props = {

}

const DockOffset = ({}: Props) => {
    return (
        <div>
            DockOffset
            
        </div>
    )
}

export default DockOffset
